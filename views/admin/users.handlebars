{{!--

login form handlebar template

--}}

<h1>{{title}}</h1>

<h3>We have {{n}} users.</h3>

<h3>List of Users:</h3>
<div id="userList">
    <ul>
        {{#each users}}
        <li><a href="/admin/users/{{userID}}">{{userFirstName}} {{userLastName}}</a></li>
        {{/each}}
    </ul>
</div>

<a href="/admin/home">Home</a>